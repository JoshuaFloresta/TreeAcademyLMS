import { Check, FileSignature } from 'lucide-react'
import PrimaryButton from '../PrimaryButton.jsx'

export default function AgreementStep({ application, signed, setSigned, onContinue, onBack }) {
  return <><p className="eyebrow">STEP 1 OF 4 · ENROLLMENT REVIEW</p><h1>Review your<br /><em>enrollment details.</em></h1><p className="lead">Your legal details will be collected in the full PASS-FIRST application immediately after this review.</p><div className="agreement-preview"><div className="agreement-top"><span className="agreement-file"><FileSignature /> Tree Academy Enrollment</span><span>Before application</span></div><div className="agreement-body"><p className="doc-kicker">LEARNER ENROLLMENT REVIEW</p><h3>Welcome to Tree Academy</h3><p>Your application, document forms, and electronic signatures are stored with your enrollment and supplied as signed PDFs to the academy.</p>{application && <div className="doc-fields"><span><small>LEARNER NAME</small><strong>{application.name}</strong></span><span><small>EMAIL ADDRESS</small><strong>{application.email}</strong></span></div>}<div className="signature-line"><span><small>STATUS</small><strong>{signed ? 'Ready to begin application' : 'Awaiting confirmation'}</strong></span><FileSignature size={18} /></div></div></div><label className="consent-check"><input type="checkbox" checked={signed} onChange={(event) => setSigned(event.target.checked)} /><span><Check size={13} /></span>I have reviewed the enrollment process and consent to continue with the digital application and electronic signatures.</label><div className="button-row"><button type="button" className="button button-ghost" onClick={onBack}>Back</button><PrimaryButton type="button" disabled={!signed} onClick={onContinue}>Review agreement &amp; enroll</PrimaryButton></div></>
}
